import { atom } from "jotai";

export const edLevelAtom = atom(undefined);

export const dreamSchoolAtom = atom(undefined);

export const dreamMayorAtom = atom(undefined);

export const linkedInAccountAtom = atom(undefined);
